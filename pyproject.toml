[project]
name = "distributed-inference-orchestrator"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn",
    "httpx",
    "pydantic",
    "typer",
]

[tool.setuptools.packages.find]
include = ["agent*", "common*", "registry*", "planner*"]
